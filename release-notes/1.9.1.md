### Fixes

- Recognize OpenSSH and other application profiles in verbose UFW output, including IPv6. Preserve source-specific Fail2Ban rules.
- Accept repeated DNS entries for the same proxy endpoint. Continue rejecting unexpected servers and ports.
- Replace earlier global resolver entries when configuring dnsproxy and report success only after routing verification completes.
- Clearly report restored firewall and DNS settings after a failed transaction.
- Name conflicting Fish aliases without displaying generated verification code.

### Validation

Regression tests cover the reported UFW and DNS output formats. CI also exercises real UFW, systemd-resolved, dnsproxy, firewall confirmation, and rollback in isolated Ubuntu 24.04 and 26.04 containers.
